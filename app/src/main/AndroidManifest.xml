<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.rossen.androidadvancedlearning">

    <uses-feature
        android:name="android.hardware.camera"
        android:required="true" />

    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <provider
            android:name="android.support.v4.content.FileProvider"
            android:authorities="com.example.android.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>
        <activity android:name="com.example.rossen.androidadvancedlearning.fragments.ui.AndroidMeActivity"/>
        <activity android:name="com.example.rossen.androidadvancedlearning.mainpackage.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity android:name="com.example.rossen.androidadvancedlearning.fragments.ui.MainFragmentsActivity"/>

        <activity android:name=".libraries.EmojifyMeActivity"/>
        <activity android:name=".squaker_cloud.MainSquakerActivity"/>
        <activity android:name=".squaker_cloud.following.FollowingPreferenceActivity" />
        <activity android:name=".shush_me.ShushMainActivity" />
        <activity android:name=".users.UserActivity" />
        <activity android:name=".mvp.MVPMainActivity" />

        <service android:name=".squaker_cloud.fcm.SquawkFirebaseMessageService" >
        <intent-filter>
            <action android:name="com.google.firebase.MESSAGING_EVENT" />
        </intent-filter>
        </service>

    </application>

</manifest>